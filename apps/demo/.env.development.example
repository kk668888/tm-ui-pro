# 复制本文件为 .env.development 并按需调整（仅开发环境生效）。

# ── Mock 演示配置 ─────────────────────────────────────────
# Mock 接口签发的 access token 有效期（秒），用于演示 Token 无感续期：
# - 120（默认，2 分钟）：小于刷新阈值（5 分钟），每次请求都会触发一次主动刷新，
#   能在 Network 面板直观看到续期动作；
# - 调大（如 600 = 10 分钟，> 刷新阈值）：只在 token 临过期时刷新，接近真实节奏；
# - 调小（如 30）：加速演示「闲置过期 → 401 兜底刷新」。
VITE_MOCK_ACCESS_TTL_SEC=120

# ── DEV 观测面板 ───────────────────────────────────────────
# 是否开启「Token 续期」DEV 悬浮面板（右下角）。仅开发环境生效，生产不打包。
# - 默认关闭（不展示）；设 true 才开启；
# - 不设 / 设非 'true' 值：关闭（面板组件不进产物，界面无悬浮窗）。
VITE_DEV_TOKEN_PANEL=false

# ── DEV 登录配置 ────────────────────────────────────────
# 是否开启研发环境免登录。仅开发环境生效，生产环境始终走真实登录链路。
# - 不配置 / true：默认注入本地 dev 用户、菜单和权限，直接进入业务页面；
# - false：关闭免登录，用于调试真实登录、Token 续期、401 和 403 链路。
VITE_DEV_AUTH_BYPASS=true
